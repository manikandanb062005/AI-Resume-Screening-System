apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: resume-screening
  region: ap-south-1
  version: "1.31"
nodeGroups:
  - name: workers
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
