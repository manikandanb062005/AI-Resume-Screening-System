kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: resume-screening
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # NodePort we'll expose the frontend on
        hostPort: 8080          # accessible at localhost:8080, same as before
        protocol: TCP
